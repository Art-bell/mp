import { HeaderSwitchDirective } from './header-switch.directive';

describe('HeaderSwitchDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderSwitchDirective();
    expect(directive).toBeTruthy();
  });
});
